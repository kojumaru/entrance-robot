# 精密Lab. 受付・案内ロボット システム仕様書

## 1. プロジェクト概要

五月祭の展示「精密Lab.」において、来場者を迎え入れ、展示内容や会場マップを音声と画面で案内するロボットを開発する。

## 2. システム構成

- **ハードウェア**: ノートPC または Raspberry Pi 4/5
- **OS**: Windows / Linux / macOS
- **主なライブラリ**:
  - `OpenAI API` (GPT-4o / Gemini 1.5 Flash) : 対話・案内生成
  - `VOICEVOX` または `Google TTS` : 音声合成
  - `SpeechRecognition` (Whisper API等) : 音声認識
  - `Streamlit` または `Flask` : GUI（マップ・画像表示用）

## 3. 動作モードと切り替えロジック

システムは以下の2つの状態（ステート）を持つ。

### A. 待機モード (Standby Mode)

- **動作**: 10〜15秒に一度、ランダムに選ばれた「呼び込み用定型文」を喋る。
- **画面**: 展示のメインロゴや、呼び込み用のキャッチコピーを表示。

### B. 対話モード (Interaction Mode)

- **動作**: ユーザーが近づいたことをYOLOなどで感知（この部分は制作仲間が作ってくれるので未実装でOK。現状ではラベルがあれば良い。）してトリガーに開始。ユーザーの質問に対し、LLMが「音声」と「画像パス」を生成して回答する。

## 4. モード切り替えの仕様（実装の肝）

YOLOによる物理トリガー。これが実装されるまでは、スペースキーが押された時に切り替える形式にする。

## 5. 案内コンテンツの制御

LLM（GPT等）には以下の形式でレスポンスを出力させるようシステムプロンプトで制御する。

- **音声用テキスト**: ユーザーへの回答文。
- **画像フラグ**: 表示すべき画像ファイル名（例：`map_A.png`, `lab_photo.jpg`）。

## 6. ディレクトリ構造（想定）

後に混雑状況をfirebaseから取得してその情報をコンテキストとした誘導をさせたいので、あえてこのseimitsu-ticketディレクトリに作ってみたが、別のディレクトリにしたほうがよければそれを提案してほしい。

- `main.py`: メインのステート管理ロジック
- `assets/`: 画像ファイル（マップ、写真）
- `audio/`: 定型文の音声ファイル
- `prompts/`: システムプロンプト設定ファイル

## 7. Claudeへの依頼事項

1. 上記仕様に基づく、Pythonでのメインルーチン（whileループを用いたステート管理）のコード作成。
2. 音声認識の結果から「画像パス」と「喋る内容」を分離して取得する関数の作成。
3. 騒音対策として、マイク感度の閾値を設定できるような音声入力待機処理の実装。
